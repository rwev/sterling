Review the git diff of staged and unstaged changes to any files under `.claude/agents/` or `.claude/commands/`. If there are no changes, check recent commits since the last CHANGELOG.md entry for agent/command modifications.

Read `CHANGELOG.md` to see the existing format and entries.

Add a new dated entry at the top of the log (below the `# Changelog` header) summarizing what changed and which agent or command files were modified. Follow the format established by prior entries. Be specific about what changed in each file — don't just say "updated"; describe the substance of the change in one line per file.

If nothing has changed in agents or commands, say so and do not modify the changelog.
